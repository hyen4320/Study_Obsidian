원하는 데이터를 골라서 뽑아내는 명령어

실행순서
FROM->WHERE->GROUP BY-> HAVING->SELECT->ORDER BY
FWGHSO

### Select 다양한 표현
DISTINCT는 중복 제거
|| 는 문자열 연결
1+1 계산식

### DUAL 테이블
시스템 내장 가짜 테이블, 한 행, 한 컬럼만 있고, 계산이나 함수 결과를 확인할 때 쓴다.

