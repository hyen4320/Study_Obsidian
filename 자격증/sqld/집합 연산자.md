UNION / UNION ALL / INTERSECT / MINUS
두 개 이상의 SELECT 결과를 합치거나 비교하는 도구

UNION-합집합(중복제거-정렬 발생), UNION ALL-합집합(중복유지-가장빠름), INTERSECT-교집합(양쪽 공동만), MINUS/EXCEPT A-B(차집합) A에만 있는 것

집합 연산에서 NULL은 NULL과 같은 값으로 취급됨